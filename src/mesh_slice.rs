//指定した平面に対して交点を求める
pub fn slice(he_ds: &HalfEdgeDS, plane: [f32; 3]){
    //faceをループし、交差するfaceを探す
    for fi in 0..he_ds.face.len(){

    }
    //線分のループを一周するまたは、交差対象のfaceがなくなるまでループする

    //交差するfaceの中でまだ線分を計算してないものがある場合
}

pub fn cross_triangle_plane(face_loop: [Vertex; 3], plane: [f32; 3]) -> bool {
    //face
}

//線分と平面の交点を求める
pub fn cross_segment_plane(){

}